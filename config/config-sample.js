// Generated by CoffeeScript 1.6.3
var Extension, ExtensionUtils, config, helper, myKeybindings, myLayouts, onStartup;

ExtensionUtils = imports.misc.extensionUtils;

Extension = ExtensionUtils.getCurrentExtension();

helper = Extension.imports.helper;

onStartup = function() {
  return false;
};

myKeybindings = [];

myLayouts = [];

helper.log("helper!");

config = {
  onStartup: onStartup,
  layouts: myLayouts,
  keybindings: myKeybindings
};
